spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/springjdbc
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

  jpa:
    hibernate:
      ddl-auto: update  # 更新或者创建数据表结构
    show-sql: true  # 控制台显示sql


